<controls:MetroWindow x:Class="MySnooper.ListEditor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        Title="List Editor" Height="400" Width="330" TitlebarHeight="40" Background="Black"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize" ShowInTaskbar="False">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>

        <ListView Name="MyListView" Grid.Row="0" Background="Transparent" BorderThickness="0">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" Margin="6,3,0,3"></TextBlock>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Remove from the list" Click="RemoveFromList"></MenuItem>
                </ContextMenu>
            </ListView.ContextMenu>
        </ListView>

        <TextBox Grid.Row="1" Name="AddNewUser" Text="Add a new user to the list.." KeyDown="NewUserAdd" GotKeyboardFocus="NewUserEnter" LostKeyboardFocus="NewUserLeave"></TextBox>
    </Grid>
</controls:MetroWindow>